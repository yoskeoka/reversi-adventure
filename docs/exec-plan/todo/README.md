Place execution plans here.
Format: `<sequence>-description.md` (e.g. `0003-init.md`).
Use the next available sequence number so active plan creation order stays visible without checking Git history.
When a plan is completed and the PR is ready, delete the file. Use the
implementation PR or Git history to retrieve completed plans.
