# Plan: revise 0021 after merged benchmark foundations

> **Execution**: Use `/execute-task` to implement this plan. After implementation is complete, use `/review-task` to prepare and create the PR.

## Objective and completion boundary

After 0029 and 0030 merge, update `0021-reversi-ai-performance-benchmark.md`
to remove only the completed, Git-retrievable profiler and corpus foundations.
Keep a precise residual contract for reference analysis, comparator, true-main
baseline, CI schema tests, and documentation. Do not treat local or remote
unmerged commits as completed work.

## Verification

The amended plan names merged PR/SHA evidence for each removed section,
contains no stale eight-position/prefix assumptions, and passes `git diff
--check`.

## Addresses

- N/A.
